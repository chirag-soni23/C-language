#include<stdio.h>
#include<string.h>
int main(){
    1;// String length;
    // char name[] = "Chirag";
    // int length = strlen(name);
    // printf("Length is: %d",length);

    2;// String copy;
    // char oldStr[] = "oldstr";
    // char newStr[] = "newstr";
    // strcpy(newStr,oldStr);
    // puts(newStr);
    // puts(oldStr);

    3;// String concate
    // char firstStr[100] = "Hello";
    // char secondStr[] = " World";
    // strcat(firstStr,secondStr);
    // puts(firstStr);

    4;//String compare ASCII value
    char firstStr[] = "HHHA";
    char secondStr[] = "HHHB";
    printf("%d",strcmp(secondStr,firstStr));
    
    return 0;
}